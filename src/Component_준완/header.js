import React from "react";
import styles from "./header.module.css";

const Header = () => {
  return (
    <div className={styles.header}>
      <h2>Uber로 어디로든 이동하세요</h2>
    </div>
  );
};

export default Header;